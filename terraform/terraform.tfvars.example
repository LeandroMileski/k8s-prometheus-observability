# terraform/terraform.tfvars.example
# Copy to terraform.tfvars and fill in your values
# Never commit terraform.tfvars — it contains secrets

ssh_pubkey   = "ssh-rsa your-public-key"
region       = "us-ord"
worker_count = 1
linode_token = "your-linode-api-token"